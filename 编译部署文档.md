# 图书馆座位预约系统 - 生产环境部署指南

## 📋 目录
- [环境要求](#环境要求)
- [服务器准备](#服务器准备)
- [数据库部署](#数据库部署)
- [后端部署](#后端部署)
- [前端部署](#前端部署)
- [Nginx配置](#nginx配置)
- [端口和地址配置](#端口和地址配置)
- [系统服务配置](#系统服务配置)
- [监控和日志](#监控和日志)
- [故障排除](#故障排除)

## 🖥️ 环境要求

### 服务器配置建议
- **CPU**: 2核心以上
- **内存**: 4GB以上
- **硬盘**: 50GB以上
- **操作系统**: CentOS 7/8, Ubuntu 18.04/20.04, Windows Server 2016+

### 软件环境
- **JDK**: 1.8+
- **MySQL**: 5.7+ 或 8.0+
- **Node.js**: 14.0+ (仅构建时需要)
- **Nginx**: 1.18+ (推荐)
- **Maven**: 3.6+ (仅构建时需要)

## 🔧 服务器准备

### 1. 创建应用目录
```bash
# 创建应用根目录
sudo mkdir -p /opt/library-reservation
cd /opt/library-reservation

# 创建子目录
sudo mkdir -p {backend,frontend,logs,config,scripts}

# 设置权限
sudo chown -R $USER:$USER /opt/library-reservation
```

### 2. 安装JDK (Linux)
```bash
# CentOS/RHEL
sudo yum install java-1.8.0-openjdk java-1.8.0-openjdk-devel

# Ubuntu/Debian
sudo apt update
sudo apt install openjdk-8-jdk

# 验证安装
java -version
javac -version
```

### 3. 安装MySQL
```bash
# CentOS 7
sudo yum install mysql-server mysql
sudo systemctl start mysqld
sudo systemctl enable mysqld

# Ubuntu
sudo apt install mysql-server mysql-client
sudo systemctl start mysql
sudo systemctl enable mysql

# 安全配置
sudo mysql_secure_installation
```

### 4. 安装Nginx
```bash
# CentOS
sudo yum install nginx
sudo systemctl enable nginx

# Ubuntu
sudo apt install nginx
sudo systemctl enable nginx
```

## 🗄️ 数据库部署

### 1. 创建数据库和用户
```sql
-- 登录MySQL
mysql -u root -p

-- 创建数据库
CREATE DATABASE library_reservation DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建专用用户（生产环境建议）
CREATE USER 'library_user'@'localhost' IDENTIFIED BY 'your_secure_password';
GRANT ALL PRIVILEGES ON library_reservation.* TO 'library_user'@'localhost';
FLUSH PRIVILEGES;

-- 退出
EXIT;
```

### 2. 导入数据库结构
```bash
# 上传database_design.sql到服务器
scp database_design.sql user@server:/opt/library-reservation/

# 导入数据库
mysql -u library_user -p library_reservation < /opt/library-reservation/database_design.sql
```

### 3. 验证数据库
```sql
-- 检查表是否创建成功
USE library_reservation;
SHOW TABLES;

-- 检查初始数据
SELECT * FROM user;
SELECT * FROM library LIMIT 5;
```

## 🚀 后端部署

### 1. 编译打包
```bash
# 在开发机器上编译
cd backend
mvn clean package -DskipTests

# 生成的jar文件位于: target/library-reservation-1.0.0.jar
```

### 2. 上传到服务器
```bash
# 上传jar包
scp target/library-reservation-1.0.0.jar user@server:/opt/library-reservation/backend/

# 上传配置文件
scp src/main/resources/application-prod.yml user@server:/opt/library-reservation/config/
```

### 3. 创建生产环境配置文件
```bash
# 在服务器上创建生产配置
sudo nano /opt/library-reservation/config/application-prod.yml
```

**生产环境配置文件内容:**
```yaml
server:
  port: 8080
  servlet:
    context-path: /api

spring:
  profiles:
    active: prod
  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/library_reservation?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: library_user
    password: your_secure_password
    
    # 连接池配置
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  # 定时任务配置
  task:
    scheduling:
      pool:
        size: 4
      thread-name-prefix: library-task-

# MyBatis Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    # 生产环境关闭SQL日志
    # log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath*:/mapper/**/*.xml

# 日志配置
logging:
  level:
    com.library: info
    root: warn
  file:
    name: /opt/library-reservation/logs/application.log
  logback:
    rollingpolicy:
      max-file-size: 100MB
      max-history: 30
```

### 4. 创建启动脚本
```bash
# 创建启动脚本
sudo nano /opt/library-reservation/scripts/start-backend.sh
```

**启动脚本内容:**
```bash
#!/bin/bash

APP_NAME="library-reservation"
APP_JAR="/opt/library-reservation/backend/library-reservation-1.0.0.jar"
CONFIG_FILE="/opt/library-reservation/config/application-prod.yml"
PID_FILE="/opt/library-reservation/backend/app.pid"
LOG_FILE="/opt/library-reservation/logs/startup.log"

# JVM参数
JVM_OPTS="-Xms512m -Xmx1024m -XX:+UseG1GC -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/opt/library-reservation/logs/"

# 检查是否已运行
if [ -f "$PID_FILE" ]; then
    PID=$(cat $PID_FILE)
    if ps -p $PID > /dev/null 2>&1; then
        echo "$APP_NAME is already running (PID: $PID)"
        exit 1
    else
        rm -f $PID_FILE
    fi
fi

# 启动应用
echo "Starting $APP_NAME..."
nohup java $JVM_OPTS -jar $APP_JAR --spring.config.location=$CONFIG_FILE > $LOG_FILE 2>&1 &

# 保存PID
echo $! > $PID_FILE

echo "$APP_NAME started successfully (PID: $!)"
echo "Log file: $LOG_FILE"
```

### 5. 创建停止脚本
```bash
# 创建停止脚本
sudo nano /opt/library-reservation/scripts/stop-backend.sh
```

**停止脚本内容:**
```bash
#!/bin/bash

APP_NAME="library-reservation"
PID_FILE="/opt/library-reservation/backend/app.pid"

if [ ! -f "$PID_FILE" ]; then
    echo "$APP_NAME is not running"
    exit 1
fi

PID=$(cat $PID_FILE)

if ps -p $PID > /dev/null 2>&1; then
    echo "Stopping $APP_NAME (PID: $PID)..."
    kill $PID
    
    # 等待进程结束
    for i in {1..30}; do
        if ! ps -p $PID > /dev/null 2>&1; then
            break
        fi
        sleep 1
    done
    
    # 强制杀死进程
    if ps -p $PID > /dev/null 2>&1; then
        echo "Force killing $APP_NAME..."
        kill -9 $PID
    fi
    
    rm -f $PID_FILE
    echo "$APP_NAME stopped successfully"
else
    echo "$APP_NAME is not running"
    rm -f $PID_FILE
fi
```

### 6. 设置脚本权限并启动
```bash
# 设置执行权限
chmod +x /opt/library-reservation/scripts/*.sh

# 启动后端服务
/opt/library-reservation/scripts/start-backend.sh

# 检查服务状态
curl http://localhost:8080/api/auth/current
```

## 🌐 前端部署

### 1. 编译构建
```bash
# 在开发机器上构建
cd frontend

# 安装依赖
npm install

# 构建生产版本
npm run build

# 构建完成后，dist目录包含所有静态文件
```

### 2. 上传静态文件
```bash
# 打包dist目录
tar -czf frontend-dist.tar.gz dist/

# 上传到服务器
scp frontend-dist.tar.gz user@server:/opt/library-reservation/

# 在服务器上解压
cd /opt/library-reservation
tar -xzf frontend-dist.tar.gz
mv dist frontend/
```

### 3. 配置Nginx
```bash
# 创建Nginx配置文件
sudo nano /etc/nginx/sites-available/library-reservation
```

**Nginx配置内容:**
```nginx
server {
    listen		9527;
    server_name localhost;  # 替换为你的域名或IP
    
    # 前端静态文件
    location / {
        root library_reservation_g;
        index index.html;
    }
    
    # 后端API代理
    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 支持WebSocket (如果需要)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
```

### 4. 启用Nginx配置
```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/library-reservation /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启Nginx
sudo systemctl restart nginx
```

## ⚙️ 端口和地址配置

### 1. 修改后端端口

**方法1: 修改配置文件**
```yaml
# application-prod.yml
server:
  port: 9090  # 修改为新端口
```

**方法2: 启动时指定**
```bash
java -jar app.jar --server.port=9090
```

### 2. 修改前端开发端口

**修改 vue.config.js:**
```javascript
module.exports = {
  devServer: {
    port: 4000,  // 修改开发端口
    proxy: {
      '/api': {
        target: 'http://localhost:9090',  # 修改后端地址
        changeOrigin: true
      }
    }
  }
}
```

### 3. 修改API请求地址

**方法1: 修改 request.js (开发环境)**
```javascript
const request = axios.create({
  baseURL: 'http://your-api-server.com/api',  // 直接指定完整地址
  timeout: 10000,
  withCredentials: true
})
```

**方法2: 环境变量配置**

创建 `.env.production` 文件:
```bash
# .env.production
VUE_APP_API_BASE_URL=http://your-api-server.com/api
```

修改 `request.js`:
```javascript
const request = axios.create({
  baseURL: process.env.VUE_APP_API_BASE_URL || '/api',
  timeout: 10000,
  withCredentials: true
})
```

### 4. 数据库连接配置

**修改数据库地址和端口:**
```yaml
spring:
  datasource:
    url: jdbc:mysql://your-db-server:3306/library_reservation?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: your_username
    password: your_password
```

### 5. 跨域配置 (如果前后端不同域)

**后端跨域配置:**
```java
@Configuration
public class CorsConfig {
    
    @Bean
    public CorsFilter corsFilter() {
        CorsConfiguration config = new CorsConfiguration();
        config.setAllowCredentials(true);
        config.addAllowedOriginPattern("*");  // 生产环境建议指定具体域名
        config.addAllowedHeader("*");
        config.addAllowedMethod("*");
        
        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/**", config);
        
        return new CorsFilter(source);
    }
}
```

## 🔧 系统服务配置

### 1. 创建Systemd服务 (Linux)

```bash
# 创建服务文件
sudo nano /etc/systemd/system/library-reservation.service
```

**服务配置内容:**
```ini
[Unit]
Description=Library Reservation System
After=network.target mysql.service

[Service]
Type=forking
User=library
Group=library
WorkingDirectory=/opt/library-reservation
ExecStart=/opt/library-reservation/scripts/start-backend.sh
ExecStop=/opt/library-reservation/scripts/stop-backend.sh
PIDFile=/opt/library-reservation/backend/app.pid
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### 2. 启用系统服务
```bash
# 重载systemd配置
sudo systemctl daemon-reload

# 启用服务
sudo systemctl enable library-reservation

# 启动服务
sudo systemctl start library-reservation

# 查看服务状态
sudo systemctl status library-reservation
```

### 3. Windows服务配置

**使用NSSM (Non-Sucking Service Manager):**
```cmd
# 下载并安装NSSM
# 创建服务
nssm install LibraryReservation

# 配置服务
# Application Path: C:\Program Files\Java\jdk1.8.0_XXX\bin\java.exe
# Arguments: -jar C:\library-reservation\backend\library-reservation-1.0.0.jar --spring.config.location=C:\library-reservation\config\application-prod.yml
# Startup directory: C:\library-reservation\backend

# 启动服务
nssm start LibraryReservation
```

## 📊 监控和日志

### 1. 日志配置

**应用日志位置:**
- 应用日志: `/opt/library-reservation/logs/application.log`
- 启动日志: `/opt/library-reservation/logs/startup.log`
- Nginx日志: `/var/log/nginx/library-reservation.*.log`

**日志轮转配置:**
```bash
# 创建logrotate配置
sudo nano /etc/logrotate.d/library-reservation
```

```bash
/opt/library-reservation/logs/*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    create 644 library library
    postrotate
        systemctl reload library-reservation
    endscript
}
```

### 2. 健康检查脚本

```bash
# 创建健康检查脚本
nano /opt/library-reservation/scripts/health-check.sh
```

```bash
#!/bin/bash

API_URL="http://localhost:8080/api/auth/current"
LOG_FILE="/opt/library-reservation/logs/health-check.log"

# 检查API响应
response=$(curl -s -o /dev/null -w "%{http_code}" $API_URL)

if [ $response -eq 200 ] || [ $response -eq 401 ]; then
    echo "$(date): Service is healthy (HTTP $response)" >> $LOG_FILE
    exit 0
else
    echo "$(date): Service is unhealthy (HTTP $response)" >> $LOG_FILE
    # 可以添加重启逻辑或告警
    exit 1
fi
```

### 3. 定时健康检查

```bash
# 添加到crontab
crontab -e

# 每5分钟检查一次
*/5 * * * * /opt/library-reservation/scripts/health-check.sh
```

## 🔒 安全配置

### 1. 防火墙配置
```bash
# CentOS/RHEL (firewalld)
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --reload

# Ubuntu (ufw)
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
```

### 2. SSL证书配置 (HTTPS)

**获取Let's Encrypt证书:**
```bash
# 安装certbot
sudo yum install certbot python3-certbot-nginx  # CentOS
sudo apt install certbot python3-certbot-nginx  # Ubuntu

# 获取证书
sudo certbot --nginx -d your-domain.com
```

**Nginx HTTPS配置:**
```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;
    
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    # SSL配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    
    # 其他配置同HTTP版本...
}

# HTTP重定向到HTTPS
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}
```

## 🚨 故障排除

### 1. 常见问题

**后端启动失败:**
```bash
# 检查日志
tail -f /opt/library-reservation/logs/application.log

# 检查端口占用
netstat -tlnp | grep 8080

# 检查Java进程
ps aux | grep java
```

**前端访问404:**
```bash
# 检查Nginx配置
sudo nginx -t

# 检查文件权限
ls -la /opt/library-reservation/frontend/

# 检查Nginx错误日志
tail -f /var/log/nginx/error.log
```

**数据库连接失败:**
```bash
# 检查MySQL服务
sudo systemctl status mysql

# 测试数据库连接
mysql -u library_user -p -h localhost library_reservation

# 检查防火墙
sudo iptables -L | grep 3306
```

### 2. 性能优化

**JVM调优:**
```bash
# 修改启动脚本中的JVM参数
JVM_OPTS="-Xms1g -Xmx2g -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+UnlockExperimentalVMOptions -XX:+UseJVMCICompiler"
```

**数据库优化:**
```sql
-- 添加索引
CREATE INDEX idx_reservation_user_time ON reservation(user_id, start_time, end_time);
CREATE INDEX idx_reservation_seat_time ON reservation(seat_id, start_time, end_time);

-- 优化配置 (my.cnf)
[mysqld]
innodb_buffer_pool_size = 1G
innodb_log_file_size = 256M
max_connections = 200
```

**Nginx优化:**
```nginx
# 在http块中添加
worker_processes auto;
worker_connections 1024;

gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

# 启用缓存
location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

## 📋 部署检查清单

### 部署前检查
- [ ] 服务器环境准备完成
- [ ] 数据库安装并配置完成
- [ ] JDK安装并配置环境变量
- [ ] Nginx安装并配置完成
- [ ] 防火墙规则配置正确

### 部署过程检查
- [ ] 数据库导入成功
- [ ] 后端jar包上传并配置正确
- [ ] 前端静态文件上传并配置正确
- [ ] Nginx配置文件语法正确
- [ ] 系统服务配置正确

### 部署后验证
- [ ] 后端API接口正常响应
- [ ] 前端页面正常访问
- [ ] 用户登录功能正常
- [ ] 预约功能正常
- [ ] 定时任务正常运行
- [ ] 日志记录正常

---

## 📞 技术支持

如遇到部署问题，请检查：
1. 系统日志: `/opt/library-reservation/logs/`
2. Nginx日志: `/var/log/nginx/`
3. 系统服务状态: `systemctl status library-reservation`
4. 网络连接: `netstat -tlnp`

**部署完成后访问地址:**
- 前端首页: `http://your-domain.com`
- 移动端: `http://your-domain.com/#/mobile`
- 管理端: `http://your-domain.com/#/admin`
- API接口: `http://your-domain.com/api`

**默认管理员账号:**
- 用户名: admin
- 密码: admin

> ⚠️ **安全提醒**: 部署完成后请立即修改默认管理员密码！